/**
 * pair: a data structure to hold an ordered pair of an (int, double)
 */
public class Pair {
    int upper;
    double rate;
    //constructor
    Pair(int upper,Double rate ){
        this.upper = upper;
        this.rate = rate;
    }
}
